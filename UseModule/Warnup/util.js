const capsLock = (string) => {
  return string.toUpperCase();
};

const square = (number) => {
  return number * number;
};

const greetings = (name) => {
  return `Hello ${name}`;
};

export { capsLock, square };
export default greetings;
